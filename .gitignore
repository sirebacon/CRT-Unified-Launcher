.codex/
AI_GIT_ACCESS.md

# Python cache artifacts
__pycache__/
*.py[cod]

# Session/runtime state files
runtime/
.session.lock
wrapper_stop_enforce.flag

# Local config overrides (may contain cookies/secrets — never commit)
crt_config.local.json

# Local debug/log outputs
*_debug.log
*.log
